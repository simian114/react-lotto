import styled from 'styled-components';

const AppWrapper = styled.div`
  padding: 0.75rem;
  .title {
    text-align: center;
  }
`;

const AppFlexWrapper = styled.div`
  display: flex;
  justify-content: center;
  margin-top: 1.25rem;
  width: 100%;
`;

const AppWidthWrapper = styled.div`
  width: 100%;
`;

export { AppWrapper, AppFlexWrapper, AppWidthWrapper };
